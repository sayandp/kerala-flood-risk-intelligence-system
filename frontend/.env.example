# Copy to `frontend/.env` and adjust. `.env` is gitignored.
#
# Base URL of the FastAPI backend. Anything prefixed with VITE_ is
# inlined into the client bundle at build time and is publicly
# visible — never put a secret here.
VITE_API_URL=http://localhost:8001
